[package]
name = "zopfli-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Rust wrapper around the zopfli crate, mapping C ZopfliOptions defaults"

[dependencies]
zopfli = { version = "0.8", features = ["gzip", "zlib"] }

[dev-dependencies]
flate2 = "1"
