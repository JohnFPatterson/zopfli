[package]
name = "zopfli-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Rust Zopfli core: C-compatible squeeze/LZ77 plus gzip/zlib wrappers"

[dependencies]
zopfli = { version = "0.8", features = ["gzip", "zlib"] }

[dev-dependencies]
flate2 = "1"
