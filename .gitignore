# Rust build artifacts
/rust/target/
# Per-crate fallback (Agent 2 may emit libzopflipng.a outside the workspace)
rust/*/target/

# Cargo backup / incremental junk
**/*.rs.bk

# C object files from the Makefile
*.o
obj/
